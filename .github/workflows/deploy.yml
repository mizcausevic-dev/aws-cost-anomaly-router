name: Deploy to awsfinops.kineticgain.com

# Static HTML -> Hostinger FTP at /awsfinops/
# workflow_dispatch-only: subdomain not yet in Hostinger DNS.

on:
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: deploy-aws-cost-anomaly-router
  cancel-in-progress: false

jobs:
  deploy:
    name: FTP-sync to awsfinops.kineticgain.com
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v6

      - name: Verify build inputs
        run: |
          test -f index.html || (echo "FAIL: index.html missing" && exit 1)
          grep -q "AWS Cost Anomaly Router" index.html || (echo "FAIL: index.html missing title" && exit 1)
          echo "OK: verified"

      - name: FTP Deploy
        uses: SamKirkland/FTP-Deploy-Action@v4.3.5
        with:
          server: ${{ secrets.FTP_SERVER }}
          username: ${{ secrets.FTP_USERNAME }}
          password: ${{ secrets.FTP_PASSWORD }}
          local-dir: ./
          server-dir: /awsfinops/
          exclude: |
            **/.git*
            **/.git*/**
            **/.github/**
            **/node_modules/**
            README.md
            CHANGELOG.md
            LICENSE
            SECURITY.md
            CODE_OF_CONDUCT.md
            .gitignore
